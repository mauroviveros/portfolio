<div aria-hidden="true" class="fixed inset-0 -z-50 overflow-hidden">
  <!-- #101318 -->
  <div
    class:list={[
      'absolute inset-0',
      'bg-[linear-gradient(rgba(255,255,255,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.03)_1px,transparent_1px)]',
      'bg-size-[20px_20px]',
      'sm:bg-size-[30px_30px]',
      'lg:bg-size-[40px_40px]',
    ]}
  >
  </div>

  <div
    class:list={[
      'absolute top-1/4 left-1/3 rounded-full',
      'h-48 w-48 sm:h-64 sm:w-64 lg:h-96 lg:w-96',
      'blur-[60px] sm:blur-[90px] lg:blur-[120px]',
      'bg-primary/10',
    ]}
  >
    <!-- TODO: animate with framer-motion or similar library
      animate={{ x: [0, 200, 0], y: [0, -150, 0] }}
      transition={{ duration: 40, repeat: Infinity, ease: "linear" }}
    -->
  </div>

  <div
    class:list={[
      'bg-secondary/10 absolute right-1/4 bottom-1/4',
      'h-44 w-44 sm:h-60 sm:w-60 lg:h-92 lg:w-92',
      'blur-[60px] sm:blur-[90px] lg:blur-[120px]',
      'bg-emerald-500/10',
    ]}
  >
    <!-- TODO: animate with framer-motion or similar library
      animate={{ x: [0, -200, 0], y: [0, 150, 0] }}
      transition={{ duration: 50, repeat: Infinity, ease: "linear" }}
    -->
  </div>
</div>
